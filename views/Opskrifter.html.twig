<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Opskrifter</title>
</head>
<body>
{% include 'nav.php' %}
<a href="NyOpskrift.php">Ny Opskrift</a>
<h3>Opskrifter</h3>

<ul>
    {% for opskrift in Opskrifter %}
        <li>
            <p>
                <a href="LæsOpskrift.php?id={{ opskrift.Id }}">{{ opskrift.Titel }}</a></br>
                Ingredienser : {{ opskrift.Ingredienser }}</br>
                Opskrift : {{ opskrift.Opskrift }}</p></li>
    {% endfor %}
</ul>
<form method="post">
    <input type="number" name="DeleteOpskriftId" placeholder="Id" title="Skriv Id på den opskrift du vil slette" required>
    <input type="submit" name="DeleteOpskriftSubmit" value="Slet Opskrift">
</form>
</body>
</html>